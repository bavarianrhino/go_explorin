export const UserReducer = (

	state = {
        loading: false,
        currentUser: null,
        longitude: null,
        latitude: null
        
	}, action) => {

        switch (action.type) {

			default:
				return state;
		}
};